# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
*.local

# dist/ IS committed on purpose: `labgo view` serves this prebuilt bundle so
# using the viewer doesn't require Node at runtime, only at build time when
# the frontend itself changes (see README).

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Synced from ../data/*.json (gitignored, regenerable) by scripts/sync-data.mjs
public/graph.json
public/cochange.json
